1. ai_guess.py文件主要实现区别图片是人，猫还是狗；
   使用的是输入层到输出层单层的网络模型；
   训练好模型后对images文件夹下的图片进行测试；
2. functions.py文件存放经常需要用到的几个函数
3. 有想写一个三层(两层隐藏层)的神经网络的类[three_layer_network.py]，
   实现了一部分，不知道计算权重梯度的函数是否要写到这个类中，
   还是写到专门针对某个问题进行梯度下降法优化模型的文件中。